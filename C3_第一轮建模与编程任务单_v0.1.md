# C3 第一轮建模与编程任务单

状态：C3-v0.1 未冻结建模草案。仅供建模手与编程手第一轮计算和复核，不交论文手，不启动 C4。

## 1 任务结论

C3 建议沿用 C2 的连续型线性混合模型派生达标概率作为主框架，但把采血前可获得的母体因素纳入候选模型，再按各 BMI 组的真实母体构成对个体达标概率进行标准化平均。直接二项概率模型仅作稳健性对照。

本轮不预先指定 K4，也不继承 C2 的切点作为 C3 结论。需要在统一口径下重新比较 K=3、4、5，C2 的 K4 结果仅作为基准对照。

## 2 冻结输入和样本口径

主输入只允许使用 C1 冻结文件：

- `results/C1-v1.0/c1_clean_events.csv`
- `results/C1-v1.0/c1_technical_replicates.csv`
- `code/c1_model_analysis.py`

其 SHA256 必须分别为：

- `09b146af6fba3adf620ca051cca32d3bf506d5b8a4e2dfa9481845213a805444`
- `21a8a1834f1876e5f34bab666a99d1cdde65a7e58746298bffa793d92ba70901`
- `e0d98f0f6393b80c5e8054179deaf850cadac90b1b70a3afe0457e318c4cec92`

筛选条件：`keep_for_main_model=True`、男胎、孕周在 11 至 25 周、孕周和 Y 染色体浓度有效。不得重新清洗或覆盖 C1 文件。

当前核验样本为 1051 个独立采血事件、267 名孕妇；每人 1 至 7 次检测，中位数 4 次，260 人有重复检测。事件级 Y 浓度达标率为 86.68%，258 人至少一次达标。

## 3 为什么不直接用最早一次观测达标

267 人中，217 人首次观测已经达标，41 人属于区间删失，9 人截至最后一次仍未达标。另有 35 人出现过“先达到 4%，后续又低于 4%”的反转。

因此，首次观测达标时间同时受到左删失、区间删失和检测波动影响，不能把首次达标当作无误差、不可逆的真实时间。C3 主模型仍利用全部重复浓度数据建立连续型混合模型，再计算任意孕周的达标概率；区间删失结果只作辅助说明或敏感性验证。

## 4 母体变量定义和防止信息泄漏

每名孕妇的决策变量均取第一次有效采血记录：

- `baseline_bmi`：首次有效检测的 BMI，继续作为分组变量；
- `baseline_weight`：首次有效检测的体重；
- `baseline_age`、`baseline_height`；
- `pregnancy_count`：将“≥3”数值化为 3，并保留“3 表示至少 3 次”的说明；
- `birth_count`；
- `assisted_reproduction`。

年龄和身高在少量重复记录中不一致，主分析使用首次记录以符合真实决策时可获得信息；以母体内中位数或众数替代，作为数据口径敏感性分析。

不得将下列检测后才能获得的变量放入主时点决策模型：原始读段数、比对率、重复读段率、唯一比对读段数、GC 含量、过滤率、各染色体 Z 值及浓度、非整倍体结果和出生后健康结果。这些变量进入主模型会造成采血前决策的信息泄漏。它们只能用于测序质量描述或独立敏感性分析。

辅助生殖孕妇只有 4 人，主模型不估计其独立系数；仅报告样本量，并在可收敛时作敏感性项。

BMI、体重和身高之间存在函数关系和严重共线性。本次预检中同时放入三者时 VIF 分别约为 238.6、377.8 和 106.8。因此主参数化使用“BMI+身高”，体重通过 BMI 定义体现；另用“体重+身高、模型中不放 BMI”作替代参数化敏感性。任何模型都不得同时无惩罚地放入 BMI、体重和身高三个变量。

## 5 主模型和候选模型

设孕妇 i 在孕周 t 的 Y 染色体浓度为 Y_i(t)，采用随机截距模型

`Y_i(t)=mu_i(t)+u_i+epsilon_i(t)`，

其中 `u_i~N(0,sigma_u^2)` 表示孕妇间异质性。候选固定效应如下：

- M0：`week_c + baseline_bmi_c`，即 C2 基准模型；
- M1：M0 加 `baseline_age_c + baseline_height_c + pregnancy_count_c + birth_count_c`；
- M2：M1 加 `week_c^2 + baseline_bmi_c^2 + week_c*baseline_bmi_c`；
- M3：以 `baseline_weight_c + baseline_height_c` 替代 M1 中的 `baseline_bmi_c + baseline_height_c`，检验身体尺度参数化；
- M4：在最终固定效应模型上增加孕周随机斜率，仅作敏感性，不自动成为主模型。

所有连续变量按训练折均值中心化，必要时标准化。二次项和交互项必须由同一训练折的中心化变量构造。预测 11 至 25 周时，检查每名孕妇的预测均值是否随孕周非减；如果非线性模型产生明显反常下降，不得将其作为主模型。

## 6 模型选择

使用按 `mother_id` 划分的 5 折 GroupKFold，任何孕妇的记录不得跨训练集和验证集。

比较指标：

1. 主指标：由连续模型派生的 `Y>=0.04` 概率的 Brier score；
2. 次指标：log loss、校准截距和斜率、AUC；
3. 连续结果指标：Y 浓度的 RMSE 和 MAE；
4. 收敛率、参数符号和母体聚类 bootstrap 稳定性。

使用 one-standard-error 规则选择最简模型：若复杂模型的平均 Brier score 没有超过基准模型一个标准误的改进，则保留较简单模型。不得为了体现“综合因素”而强行保留无预测增益或不稳定的变量；应在结果中说明这些因素已经检验但未获得稳定增益。

当前不带混合效应的母体分组交叉验证预检结果为：M0 的平均 Brier 约 0.1133，M1 约 0.1146，M2 约 0.1149；新增因素暂未显示稳定增益。因此代码手必须严格执行模型比较，不能默认 M1 或 M2 一定优于 C2。

## 7 达标概率与检测误差

沿用 C2 的技术重复误差估计：

- `sigma_m=0.0047336048`；
- bootstrap 95% 区间约为 `[0.0033687715, 0.0060003084]`；
- 技术重复事件只有 18 个，必须保留“不确定性较大”的限制说明。

将混合模型总残差拆为

`sigma_b^2=max(sigma_e^2-sigma_m^2,0)`。

在检测误差倍数 kappa 下，随机截距主模型的边际达标概率为

`p_i(t;kappa)=Phi((mu_i(t)-0.04)/sqrt(sigma_u^2+sigma_b^2+kappa^2*sigma_m^2))`。

若做随机斜率敏感性，应将 `sigma_u^2` 替换为孕周 t 下的 `z(t)'Gz(t)`，不得仍按常数随机效应方差计算。

主口径取 `kappa=1`。检测误差敏感性取 `kappa={0,0.5,1,1.5,2}`；另将 `sigma_m` 的置信区间上下限代入，报告误差估计本身的不确定性。

直接二项模型使用与最终主模型相同的采血前变量，采用按孕妇聚类的 GEE-logit 或等价的母体聚类二项模型，作为稳健性对照。它不因 Brier score略优而自动替代 LMM 主模型；如其推荐时点明显更保守，应如实报告模型口径差异。

## 8 BMI 分组和最佳时点

候选分组数为 K=3、4、5，BMI 切点限制在 0.5 的网格上。显示范围统一为 `[20.5,47.5]`，区间采用左闭右开，最后一组右端闭合。

组约束沿用 C2：每组至少 35 名孕妇、100 个事件、10 名曾达标孕妇。若数据范围导致边缘显示端点与实际最小值不同，必须同时保存实际最小值和论文显示端点。

对 BMI 组 g，在每个候选孕周 t，将组内每名孕妇的年龄、身高、孕产史等真实基线构成带入模型，计算标准化组平均概率

`p_g(t)=mean_i[p_i(t)]`。

不得只把除 BMI 外的变量固定在总体均值，因为这样会抹掉 C3 要求考虑的组内多因素构成。

使用母体整簇 bootstrap：按孕妇有放回抽样，保留该孕妇全部检测事件；每次重新拟合模型，并使用该次重抽样后的完整母体特征构成计算组平均概率。只使用严格收敛迭代。主 bootstrap 尝试 1000 次，并同时输出尝试数、严格收敛数和失败原因。

`LCB95_g(t)` 定义为严格收敛 bootstrap 中 `p_g(t)` 的 2.5% 分位数。可靠性硬约束继续取 `LCB95_g(t)>=0.90`。

时间损失与 C2 保持一致：

- `L(t)=0`，当 `t<=12`；
- `L(t)=(t-12)/13`，当 `12<t<=25`。

组风险定义为 `R_g(t)=1-p_g(t)+L(t)`。每组在满足可靠性硬约束的孕周中选择风险最小的最早时点：

`T_g=argmin R_g(t), t in {11,11.5,...,25}, LCB95_g(t)>=0.90`。

若集合为空，必须写为“25 周内可靠性未决/建议个体化复查”，不得强行给 25 周时点，也不得把检测决策风险写成临床病理风险。

分别输出 K=3、4、5，不在程序中自动冻结最终 K。建模手根据可行组覆盖率、组内风险、推荐时点、切点 bootstrap 稳定性和简约性进行最终选择。C2 的 K4 切点 `[29.5,31.5,34.5]` 作为固定基准同时计算，以回答多因素模型相对 C2 是否真正改变分组或时点。

## 9 验证和敏感性

必须完成：

- 母体级 5 折交叉验证；
- 固定最终候选切点的 1000 次母体 bootstrap 概率和时点区间；
- 1000 次母体 bootstrap 重新选择 K 和切点，统计 K 选择频率与切点频率；
- `kappa={0,0.5,1,1.5,2}` 检测误差敏感性；
- `sigma_m` 置信区间上下限敏感性；
- 首次基线协变量与母体内中位数或众数协变量口径敏感性；
- LMM 主模型与直接二项 GEE 模型对照；
- C3 推荐结果与 C2 冻结 K4 结果对照。

区间删失达标时间可作为辅助验证，但鉴于 217 人左删失、仅 41 人提供内部达标区间且存在 35 人反转，本轮不把区间删失生存模型设为主模型。

## 10 随机种子

- 模型交叉验证和初始拆分：3301；
- 固定分组主 bootstrap：3302；
- K 与切点稳定性 bootstrap：3303；
- 检测误差敏感性：3304。

所有表格和 manifest 必须记录实际使用的种子、尝试次数、严格收敛次数、软件版本和输入文件哈希。

## 11 第一轮必须输出的表

建议目录：`results/C3-v0.1/01_first_round/`。

1. `c3_sample_audit.csv`：事件数、母体数、重复次数、删失类型和反转次数；
2. `c3_baseline_covariate_audit.csv`：母体级变量分布、异常值和母体内不一致计数；
3. `c3_collinearity.csv`：相关系数、VIF 和身体尺度参数化说明；
4. `c3_model_comparison.csv`：各候选模型 AIC/BIC、GroupKFold 指标和收敛情况；
5. `c3_model_coefficients.csv`：系数、标准误、95% 区间、P 值和标准化效应；
6. `c3_cv_fold_results.csv`：每折的母体数、事件数及预测指标；
7. `c3_technical_error.csv`：沿用并核验技术误差；
8. `c3_k_compare_summary.csv`：K=3、4、5 的切点、覆盖率、可行组数和风险摘要；
9. `c3_group_candidates.csv`：全部满足样本量约束的候选分组；
10. `c3_reach_probability_by_group_week.csv`：主模型各组各孕周概率和 LCB；
11. `c3_recommended_timing.csv`：各组最终状态、时点、概率、LCB 和风险；
12. `c3_bootstrap_status.csv`：每次拟合的严格收敛状态与失败原因；
13. `c3_bootstrap_timing_summary.csv`：时点分布及 95% 区间；
14. `c3_boundary_stability_frequency.csv`：K 和各切点的选择频率；
15. `c3_detection_error_sensitivity.csv`：kappa 与 sigma_m 区间敏感性；
16. `c3_covariate_definition_sensitivity.csv`：首次值与中位数或众数口径对照；
17. `c3_binary_model_sensitivity.csv`：GEE 对照结果；
18. `c3_vs_c2_comparison.csv`：C3 与 C2 固定 K4 的分组和时点差异；
19. `c3_run_manifest.json`。

原始 bootstrap 概率明细可以单独压缩保存，但汇总表必须能独立复核。

## 12 第一轮图件

先生成用于建模手复核的草图，不做正式论文美化：

- 多因素标准化效应或边际效应图；
- 各 BMI 组 `p_g(t)` 与 `LCB95_g(t)` 曲线；
- K=3、4、5 分组与推荐时点对照图；
- 检测误差敏感性图；
- K 与切点 bootstrap 稳定性图；
- C2 与 C3 推荐时点对照图。

每张图必须同步保存源 CSV。

## 13 编程手回传时必须回答

1. M0、M1、M2、M3 中哪个按 one-standard-error 规则被选为主模型，为什么；
2. 年龄、身高、孕产史是否提供稳定的预测增益，还是仅有解释性效应；
3. K=3、4、5 各有多少组在 25 周内满足 `LCB95>=0.90`；
4. 最终候选 K 和切点是否稳定，不能只给单次最优切点；
5. C3 相比 C2 的分组或时点是否实质改变；
6. 最高 BMI 组是否仍未决；
7. 检测误差及其估计不确定性最多使推荐时点移动多少周；
8. 所有 bootstrap 的尝试数、严格收敛数和失败数。

第一轮结果保持 `draft`，未经建模手复核不得改为 `frozen`，不得交论文手正文。
