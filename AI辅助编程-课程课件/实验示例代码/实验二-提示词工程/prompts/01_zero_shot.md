# 提示词 A · 零样本（Zero-shot）

> 直接用指令描述任务，不给任何示例，依赖模型已有能力。

```text
用 Python 写一个函数，对学生列表按分数从高到低排序，返回排序后的列表。
```

## 预期问题（用于对比）
- 未说明“分数相同怎么办”——结果顺序不确定。
- 未说明“是否修改原列表”——可能就地排序。
- 未要求类型注解、文档字符串与边界处理。
- 输出风格不稳定，难以直接用于工程。

> 把模型生成的结果保存下来，用 `evaluate.py` 评测功能正确性，并记录到优化日志。
