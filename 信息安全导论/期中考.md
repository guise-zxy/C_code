### 一、选择题

1. 关于DES 算法，除了 _____ 以外，下列描述DES 算法子密钥产生过程是正确的：
    
    A. 首先将DES算法所接受的输入密钥K(64位)，去除奇偶校验位，得到56位密钥（即经过PC - 1置换，得到56位密钥）
    
    B. 在计算第i轮迭代所需的子密钥时，首先进行循环左移，循环左移的位数取决于i的值，这些经过循环移位的值作为下一次循环左移的输入
    
    C. 在计算第i轮迭代所需的子密钥时，首先进行循环左移，每轮循环左移的位数都相同，这些经过循环移位的值作为下一次循环左移的输入
    
    D. 然后将每轮循环移位后的值经PC - 2置换，所得到的置换结果即为第i轮所需的子密钥Ki
    
2. 采用仿射密码c≡11m + 6(mod26)加密后产生的密文是WELCYLK，解密密钥和明文是：
    
    A. （15，6），sorrows
    
    B. （19，6），sorcery
    
    C. （13，6），socialy
    
    D. （17，6），society
    
3. AES加密算法中若明文长度为128比特，密钥长度为192（Nk = 6）比特时，则共需要迭代多少轮，第i轮的轮密钥是什么？以下答案正确的是
    
    A. 14，第i轮的轮密钥是W[8 * i]到W[8 * (i + 1) - 1]
    
    B. 12，第i轮的轮密钥是W[4 * i]到W[4 * (i + 1) - 1]
    
    C. 10，第i轮的轮密钥是W[4 * i]到W[4 * (i + 1) - 1]
    
    D. 14，第i轮的轮密钥是W[6 * i]到W[6 * (i + 1) - 1]
    
4. 分组密码设计原则中，所设计的密码应使用使得密钥和明文以及密文之间的依赖关系相当复杂以至于这种依赖性对密码分析者来说是无法利用，称为 _____ 原则。
    
    A. 可逆
    
    B. 混淆
    
    C. 扩散
    
    D. 置换
    
5. DES 算法中扩展运算E的功能是： _____
    
    A. 对16位的数据组的各位进行选择和排列，产生一个32位的结果
    
    B. 对32位的数据组的各位进行选择和排列，产生一个48位的结果
    
    C. 对48位的数据组的各位进行选择和排列，产生一个64位的结果
    
    D. 对56位的数据组的各位进行选择和排列，产生一个64位的结果
    
6. 在AES密码体制中，AES选取的不可约多项式为m(x)=x^8 + x^4 + x^3 + x + 1 ， GF(2^8)上的两个元素0X57乘以0X83的结果为k =
    
    A. 0XC1
    
    B. 0XE7
    
    C. 0X05
    
    D. 0X02
    
7. 根据所依据的数学难题，除了 _____ 以外，公钥密码体制可以分为以下几类：
    
    A. 模幂运算问题
    
    B. 大整数因子分解问题
    
    C. 离散对数问题
    
    D. 椭圆曲线离散对数问题
    
8. RSA算法可用于加密，现A拥有一对密钥PKA和SKA，B拥有一对密钥PKB和SKB。如果A向B发送消息，A对发送的消息进行加密的密钥是_____，B接收到密文后进行解密的密钥是 _____ 。
    
    A. PKA SKB
    
    B. PKB SKA
    
    C. PKB SKB
    
    D. SKB PKB
    
9. 为了数据传输时不发生数据截获和信息泄密，采取了加密机制。这种做法体现了信息安全的 _____ 属性。
    
    A. 保密性
    
    B. 完整性
    
    C. 可靠性
    
    D. 可用性
    
10. 以下哪个选项不属于被动攻击的是：
    
    A. 窃听
    
    B. 截收
    
    C. 分析
    
    D. 插入
    

### 二、填空题

1. 若明文为CHENXINGYINYUETING ，维吉尼亚加密密文是：OBWVZUHYGKZSMMVUHY， 则密钥是______。（注意答案必须全部用大写字母！！）
    
2. 密码学包括_**和**_两个分支。
    
3. 分析者除了知道已知明文攻击的基础上，攻击者可以任意制造或选择一些密文，并得到解密的明文，这种攻击称为_**；攻击者被动地截获密文并进行分析称为**_攻击。
    
4. 现代密码体制中，数据的安全基于保持____的秘密，而不是基于保持加解密算法。
    
5. 凯撒加密和维吉尼亚加密属于_**密码体制；DES/RSA属于**_密码体制。
    
6. DES算法64bit密钥中有效信息位是**bit，加解密使用同一算法，加解密唯一不同是**的使用顺序相反，在每一轮迭代中，迭代后的明文中的第**bit到第**bit是来自迭代前的明文中不做改变的部分。
    
7. 安全密钥异同性分，AES和IDEA属于_**算法，RSA和椭圆曲线属于**_算法。
    
8. 加密过程中，按明文信息输入方式分，明文成组加密的加密算法为**密码算法，与密钥流按序列直接加密（异或或其他运算）为**密码算法。
    
9. 加密时，将一个消息替换为其他消息称为_**加密；将明文位置进行改变称为**_加密；使密文和明文关系变得复杂称为_**加密；使明文每一位影响密文中的许多位称为**_加密；将上述多个方法按先后顺序联合使用称为_**；反复多轮运算称为**_运算。
    
10. 对称密码和非对称密码体制的最重要区别在于_**和**_是否一样。
    

### 三、判断题

1. 在公钥密码体制中，用户的公私钥之间是相关联的，为了保护私钥的安全性，用户的公钥是不能公开的。
    
2. 能经受住唯密文攻击的密码体制就能经受住选择密文攻击。
    
3. 篡改的攻击形式是破坏了消息的完整性。
    
4. 在分组密码中，明文和密钥的长度应足够长，至少能够抵抗穷举攻击。
    
5. 公钥密码体制的出现解决了对称密码体制的密钥分发问题，在公钥密码算法中，加密对称密钥使用的是发送方的私钥。
    

### 四、解答题

1. 在Diffie - Hellman密钥协商协议中，取素数n = 19，生成元α = 2，若用户A随机选取的数为6，用户B随机选取的数为14，则用户A和B计算出的密钥是分别是多少？
    
    答：用户A计算步骤为_**得出密钥是**_
    
    用户B计算步骤为_**得出的密钥是**_
    
2. 设有一（3，5）秘密分割方案，模值q = 17，（1）请问秘密拥有者为了分配子秘密需要构造一个几次 的多项式？（2）需要计算几个 子秘密？（3）若秘密值为s，系数a1 = 2，a2 = 7如何计算子秘密？（4）若在（3，5）秘密分割方案，知道其中的3个子密钥分别为（1，f(1)=3），（2，f(2)=4） ，（3，f(3)=15）。请恢复出秘密值。
    
    解：
    
    (1)
    
    (2)
    
    (3) f(1)= f(2)= f(3)= f(4)= f(5)=
    
    (4)由Shamir密钥恢复.png
    
    推出f(0) =
    
3. RSA算法中得知素数p = 13和q = 17，若公钥e = 5，试述如何计算得到私钥d，Alice和Bob如何进行加密和解密，此时假设需要加密的明文信息为m = 5。
    
    解：
    
    n =
    
    ∮(n)=
    
    根据e*d ≡1 mod ∮(n)
    
    又e*dmod ∮(n)≡1
    
    得出d =
    
    公钥为(n,e) =( , )
    
    加密明文m计算得出密文c =
    
    私钥为(n,d) =( , )
    
    解密公式为m =
    

### 答案

#### 一、选择题

1. **C**。在计算第i轮迭代所需的子密钥时，每轮循环左移的位数是根据i的值来确定的，并非都相同。
    
2. **B**。对于仿射密码c≡11m+6(mod26)，求逆元，11x≡1(mod26)，通过扩展欧几里得算法可得逆元为19，即解密密钥为(19,6)。密文WELCYLK，W对应22，(22−6)×19mod26=16即Q（这里原题目可能选项有笔误，按照正确计算逻辑应该是sorcery）。
    
3. **B**。AES加密算法中，明文长度为128比特，密钥长度为192比特（Nk=6）时，共需要迭代12轮，第i轮的轮密钥是W[4i]到W[4(i+1)−1]。
    
4. **B**。混淆原则是指所设计的密码应使得密钥和明文以及密文之间的依赖关系相当复杂，让密码分析者无法利用。
    
5. **B**。DES算法中扩展运算E的功能是对32位的数据组的各位进行选择和排列，产生一个48位的结果。
    
6. **B**。在GF(2^8)上，m(x)=x8+x4+x3+x+1，0X57乘以0X83的结果为0XE7。
    
7. **A**。根据所依据的数学难题，公钥密码体制可以分为大整数因子分解问题、离散对数问题、椭圆曲线离散对数问题等，模幂运算问题不是分类依据。
    
8. **C**。RSA算法中，A向B发送消息，A用B的公钥PKB加密，B用自己的私钥SKB解密。
    
9. **A**。采取加密机制防止数据截获和信息泄密，体现了信息安全的保密性属性。
    
10. **D**。插入属于主动攻击，窃听、截收、分析属于被动攻击。
    

#### 二、填空题

1. **KEY**。通过维吉尼亚密码的解密方法，用密文和明文对应位置字符，结合字母表顺序计算得出密钥为KEY。
    
2. **密码编码学**、**密码分析学**
    
3. **选择密文攻击**、**唯密文**
    
4. **密钥**
    
5. **古典**、**现代**
    
6. **56**、**子密钥**、**1**、**8**
    
7. **对称**、**非对称**
    
8. **分组**、**流**
    
9. **替换**、**置换**、**混淆**、**扩散**、**组合**、**迭代**
    
10. **加解密算法**、**密钥**
    

#### 三、判断题

1. **错误**。在公钥密码体制中，用户的公钥是可以公开的，私钥必须严格保密。
    
2. **错误**。能经受住唯密文攻击的密码体制不一定能经受住选择密文攻击，选择密文攻击的攻击能力更强。
    
3. **正确**。篡改攻击改变了消息的内容，破坏了消息的完整性。
    
4. **正确**。明文和密钥长度足够长能增加穷举攻击的难度，抵抗穷举攻击。
    
5. **错误**。在公钥密码算法中，加密对称密钥使用的是接收方的公钥。
    

#### 四、解答题

1. - 用户A计算步骤为：KA​=αxA​modn=26mod19=64mod19=7，然后K=KAxB​​modn=714mod19，72=49mod19=11，74=(72)2=112=121mod19=7，78=(74)2=72=49mod19=11，714=78×74×72=11×7×11=847mod19=11，得出密钥是11。
        
    - 用户B计算步骤为：KB​=αxB​modn=214mod19，24=16mod19=16，28=(24)2=162=256mod19=9，212=28×24=9×16=144mod19=11，214=212×22=11×4=44mod19=6，然后K=KBxA​​modn=66mod19，62=36mod19=17，64=(62)2=172=289mod19=4，66=64×62=4×17=68mod19=11，得出的密钥是11。
        
    
2. - (1) 秘密拥有者为了分配子秘密需要构造一个二次多项式，因为(k,n)=(3,5)，k−1=2。
        
    - (2) 需要计算n=5个子秘密。
        
    - (3) 若秘密值为s，系数a1​=2，a2​=7，多项式f(x)=s+a1​x+a2​x2，子秘密f(i)=s+a1​i+a2​i2（i=1,2,3,4,5）。
        
    - (4) 已知f(1)=3，f(2)=4，f(3)=15，设f(x)=s+2x+7x2，则⎩⎨⎧​s+2×1+7×12=3s+2×2+7×22=4s+2×3+7×32=15​，即⎩⎨⎧​s+2+7=3s+4+28=4s+6+63=15​，整理为⎩⎨⎧​s=−6s=−28s=−54​，用拉格朗日插值法，f(0)=∑i=13​f(i)∏j=i​i−j0−j​，f(0)=3×(1−2)×(1−3)(−2)×(−3)​+4×(2−1)×(2−3)(−1)×(−3)​+15×(3−1)×(3−2)(−1)×(−2)​=3×26​+4×−13​+15×22​=9−12+15=12，推出f(0)=12。
        
    
3. - n=p×q=13×17=221
        
    - φ(n)=(p−1)(q−1)=12×16=192
        
    - 根据e×d≡1modφ(n)，即5d≡1mod192，通过扩展欧几里得算法可得d=77，因为5×77=385，385mod192=1。
        
    - 公钥为(n,e)=(221,5)
        
    - 加密明文m计算得出密文c=memodn=55mod221=3125mod221=187
        
    - 私钥为(n,d)=(221,77)
        
    - 解密公式为m=cdmodn