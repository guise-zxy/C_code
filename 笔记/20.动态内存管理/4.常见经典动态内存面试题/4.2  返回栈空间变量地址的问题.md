![[Pasted image 20251011203814.png]]

![[Pasted image 20251011201647.png]]
能通过地址/即指针变量访问的==“非法（先不说有没有意义）”==内存空间（已经归还给操作系统了）：
==1.函数里的“局部变量”的地址。==
==2.main函数里的局部变量的地址。==

这时候，你用指针变量去访问这些地址：所访问的值都是==随机值==，，，，，或者==程序直接崩掉==


非法的：==NULL,常量（字符串常量，，比如：char * s="abcfgdhf"）;==

![[Pasted image 20251011203426.png]]