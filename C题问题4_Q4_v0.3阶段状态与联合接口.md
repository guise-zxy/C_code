# C题问题4 Q4 v0.3 阶段状态与联合接口

版本日期：2026年9月12日  
发送方：建模手  
接收方：代码手、论文手  
状态：调度与结算框架确认；价格预测主模块待唯一可复现规格；Gate0可并行，正式Gate1暂不冻结

## 1. 本文档作用

本文档合并代码手对Q4-v0.2的技术审查和论文手的独立价格预测试验，作为当前唯一状态接口。

- Q2、Q3冻结模型及数值不重新打开；
- Q4的负荷、光伏、储能、实际recourse和输出结构不更换；
- v0.2中的整体中位比例价格校正退出主模型；
- 滚动多输出Ridge作为主价格预测候选，但在特征和训练规则完整交付前不允许代码手自行猜测实现；
- 论文手独立试验数值只作为路线证据，必须由代码手独立复现后才能成为正式结果。

## 2. 当前状态矩阵

| 模块 | 当前结论 | 状态 |
| --- | --- | --- |
| Q4总体结构 | 因果价格预测＋Q2/Q3冻结调度＋附件4实际价格结算 | 已确认 |
| Q4-2调度 | 继承CQ-LP v1.2、`alpha=0.80`、42日残差窗口、实时储能recourse | 已确认 |
| Q4-3调度 | 继承Q3四时点滚动LP、共同一月暖启动、8策略和Shapley | 已确认 |
| 主结算解释 | 调整量按交付时段价格结算 | 已确认的模型假设，不称题面唯一解释 |
| 结算敏感性 | 调整量按6:00/12:00/18:00即时价格结算，并重新优化 | 已确认 |
| M0 | 上周同期价格 seasonal-naive | 基线模型 |
| M1 | 上周同期曲线乘统一日内中位比例 | 结构性无效，退出主模型 |
| M2 | 6小时分块比例Ridge | 不采用 |
| M3 | 严格因果的滚动120日轻量多输出Ridge | 主模型候选，规格待补齐 |
| ORACLE | 规划层使用当天未来实际价格 | 仅作完美信息上界 |
| result4-2 | EP/EQ只写计划购电量与计划购电费 | 已按Q2 v1.2 frozen核实 |
| result4-3 | 计划Sheet写计划费，调整Sheet写常规结算费，紧急费单列统计 | 已按Q3 v1.0核实 |
| Gate2 | 2月1日至2月7日连续测试 | 已确认 |

## 3. 已否决的M1价格校正

v0.2曾定义

```math
\widehat p_{d,k,t}=\kappa_{d,k}b_{d,t},\qquad \kappa_{d,k}>0.
```

对同一次阶段LP而言，该处理只是把整条未来价格向量乘同一个正常数。目标函数整体缩放不改变最优解集合，因此

```math
\arg\min_x \sum_t b_{d,t}f_t(x)
=\arg\min_x \sum_t \kappa_{d,k}b_{d,t}f_t(x).
```

在约束、第二阶段去退化规则不变时，M1不能改变调度策略。论文手独立仿真中M1与M0结果在机器精度下一致，与上述结构判断相符。

正式论文可以把这一点作为模型筛选过程的简短说明，但M1不进入最终主模型和主要结果表。

## 4. 价格预测模型的新定位

### 4.1 主候选、基线和上界

- `M3`：主价格预测候选；
- `M0`：上周同期价格，作为简单因果基线；
- `ORACLE`：当天未来实际价格全部已知，只衡量完美价格信息的理论改善空间。

M3的选择理由是其能够改变未来各时段之间的相对价格排序，而不是统一缩放价格曲线。论文手独立试验显示其预测误差和调度费用均优于M0，但这批结果尚未经过代码手独立实现与正式接口复现。

### 4.2 禁止利用正式评价期事后选模

M3一旦由建模接口正式定义，就固定为主价格预测模型。M0和ORACLE只作对照。

- 不得跑完2025年2月至12月后，再按全年实际费用在M0、M2、M3之间挑选冠军；
- 不得把正式评价期同时当作调参集；
- 若需要训练/验证选择，必须事先划分时间区间；本轮为控制工作量，不新增该选择流程；
- Ridge正则参数预设为`lambda=100`，`10/100/1000`只作敏感性，不能把100表述为全年事后最优参数。

## 5. M3正式实现前唯一阻塞项

“滚动120日轻量多输出Ridge”不足以保证两人实现一致。论文手需回传以下唯一可复现规格，代码手收到前不得自行补全：

1. 每个发布时点`k=0/6/12/18`的完整输入特征向量及排列顺序；
2. 预测目标是未来原始价格，还是相对M0的价格残差；
3. 每个历史训练样本的日期、输入日和目标日对应关系；
4. 滚动120日的截取规则，以及可用样本不足120日时的逐日启动规则；
5. 0:00、6:00、12:00、18:00各自输入维度和输出维度；
6. 是否中心化或标准化、是否拟合截距，以及标准化参数只能由当时训练集计算；
7. Ridge的准确目标函数或库调用参数，包括`lambda=100`与软件中参数名的对应关系；
8. Ridge产生负预测价格时的处理规则；
9. 缺失值、样本不足或求解失败时回退至M0的确定规则；
10. 预测结果与训练样本来源的逐日审计字段。

规格必须做到：代码手不接触论文手原型代码，仅凭文档即可独立复现同一预测矩阵。

## 6. 信息时序冻结口径

所有可实施价格预测必须满足：决策日`d`的训练数据只使用`i<d`的完整历史数据。

| 发布时点 | 当日可进入价格预测特征的实际period | 最大已观测period | 首个未来period |
| --- | --- | --- | --- |
| 0:00 | 无 | -1 | 0 |
| 6:00 | 0–35 | 35 | 36 |
| 12:00 | 0–71 | 71 | 72 |
| 18:00 | 0–107 | 107 | 108 |

程序每个发布时点至少输出：

```text
decision_date
decision_time
training_start_date
training_end_date
training_sample_count
max_observed_price_period
first_future_price_period
future_actual_price_used_count
model_name
lambda
```

`future_actual_price_used_count`必须为0。建议保留“未来价格乘10”的反事实泄漏测试：如果可实施预测发生变化，则时序检查失败。

## 7. 主结算解释

主模型继续采用交付时段价格。令`p[d,t]`为附件4中时段`t`的实际价格，则Q4-3常规结算为

```math
C_{d,\mathrm{regular}}
=\sum_t p_{d,t}
\left(G^0_{d,t}+1.5U_{d,t}-0.5V_{d,t}\right)\Delta t.
```

这是本文明确采用的模型假设，理由是购电量和结果模板均对应10分钟交付时段，题面没有提供独立远期调整报价序列。论文不得称其为题面唯一可能解释。

## 8. 调整时点即时价格敏感性

### 8.1 即时价格索引

敏感性假设发布时刻开始的首个10分钟价格已经公布：

- 6:00采用period 36；
- 12:00采用period 72；
- 18:00采用period 108。

这些价格是敏感性中的当期已知交易报价，但不得加入同次价格预测的历史观测窗口。例如6:00的预测特征仍只能读取period 0–35。

### 8.2 必须重新优化

即时价格敏感性不是对主策略事后换价。它必须同时替换阶段规划目标中的`U,V`价格系数并重新求解。

若最终时段`t`的调整来自发布时点`k(t)`，敏感性结算为

```math
C_{d,\mathrm{regular}}^{\mathrm{trade-time}}
=\sum_t p_{d,t}G^0_{d,t}\Delta t
+\sum_t p_{d,6k(t)}
\left(1.5U_{d,t}-0.5V_{d,t}\right)\Delta t.
```

在发布时点`k`的规划中，对未来时段必须采用相同的即时调整价格系数重新优化：

```math
\min \sum_{t\ge6k}
\left[
\widehat p_{d,k,t}G^0_{d,t}
+p_{d,6k}\left(1.5U^k_{d,t}-0.5V^k_{d,t}\right)
\right]\Delta t.
```

其中第一项对当前阶段决策是常数，但保留用于账本解释。最终必须生成新的`G^k,U^k,V^k`，不得复用主结算口径的调度结果。

## 9. Q4-2与Q4-3保持不变的部分

### 9.1 Q4-2

- 继承CQ-LP v1.2的净负荷预测、42日残差窗口、`alpha=0.80`风险锚点；
- 2025年2月1日初始SOC为6000 kWh；
- 计划终端SOC为6000 kWh；
- 实际执行允许当期储能recourse；
- 实际末端SOC跨日连续；
- M3规格复核通过后，正式规划目标拟使用其在0:00生成的价格预测；当前可先用M0完成工程回归，实际结算均使用附件4实际价格。

### 9.2 Q4-3

- 继承Q3 v1.0的四时点滚动LP、实际recourse、共同一月暖启动和8个策略；
- 一月必须在Q4价格模型下重新暖启动，不能复用Q3的2月1日SOC；
- 8个策略必须重新全年回测，不能直接沿用Q3的S111；
- M3规格复核通过后，启用某时点时同时使用该时点光伏预测和价格预测；跳过时点时两者均不更新；
- Shapley解释为光伏与价格综合更新的平均边际价值，不再称纯光伏信息价值。

## 10. 输出口径

### 10.1 result4-2.xlsx

按Q2 v1.2 frozen最终口径：

- 144格写`G*dt`；
- EP全天购电量写`sum(G*dt)`；
- EQ全天购电费写`sum(p*G*dt)`；
- EP、EQ均不含紧急购电；
- 紧急购电量写入独立Sheet，论文总费用另按计划费加5倍紧急购电费统计。

### 10.2 result4-3.xlsx

按Q3 v1.0 frozen最终口径：

- 计划购电量Sheet写`G0*dt`和计划费用；
- 调整购电量Sheet写`Gf*dt`和常规结算费；
- 紧急购电费不并入调整Sheet的全天购电费；
- 论文实际总费用为`C_regular+C_emergency`；
- SOC、4小时汇总和紧急购电物理区间沿用`period 0=0:00-0:10`。

## 11. 论文手独立原型证据

以下数值只用于支持路线选择，尚不是正式结果：

| 模型 | Q4-2全年费用/元 | Q4-3 S111全年费用/元 |
| --- | ---: | ---: |
| M0 | 15026508.8222 | 14588280.3731 |
| M1 | 未单独报告 | 14588280.3731 |
| M2 | 未单独报告 | 14589174.7443 |
| M3 | 14994986.5724 | 14551763.2986 |
| ORACLE | 14879800.5837 | 14445360.0800 |

论文手原型显示：

- M3相对M0在Q4-3节省36517.07元，约0.25032%；
- M3捕获约25.55%的Q4-3完美价格信息改善空间；
- M3在334天中有243天费用低于M0，11个正式评价月份均净节省；
- Q4-2中M3相对M0节省31522.25元，约0.20978%。

这些结果不得直接写入最终论文或result4。代码手独立复现、验证通过后，才决定是否转为正式证据。

## 12. 当前Gate安排

### 12.1 代码手现在可以执行

1. Gate0附件4维度、日期、统计量和模板审计；
2. M0上周同期基线与ORACLE隔离实现；
3. 价格预测时序审计和未来价格反事实泄漏测试；
4. Q4-2、Q4-3调度骨架及result4输出层复用检查；
5. 调整时点即时价格敏感性的重新优化接口和账本结构，但暂不生成正式结果。

### 12.2 代码手暂时不能执行

- 不得自行定义M3特征；
- 不得把论文手原型数值写入正式输出；
- 不得进入正式Gate1、连续7日或全年；
- 不得按正式评价期费用重新挑选预测模型或`lambda`。

### 12.3 论文手本轮回传

论文手只需回传第5节列出的M3唯一可复现规格，以及`lambda=10/100/1000`敏感性是否稳定。无需修改Q2/Q3调度模型，无需扩展新的价格预测候选。

### 12.4 下一版本条件

收到M3完整规格后，建模手将其合并成Q4唯一正式开工接口。下一版只补价格预测模块，不再修改本文件已经确认的调度、结算、输出和信息时序口径。

## 13. 结论边界

允许写：

- 为避免利用未来实时价格，Q4主模型采用严格因果价格预测；
- M3通过学习不同未来时段的相对价格结构影响调度；
- M0用于衡量复杂预测相对简单上周同期基线的增量价值；
- ORACLE用于衡量完美价格信息下的理论改善空间；
- 即时交易价敏感性通过重新优化检验结算解释影响。

禁止写：

- M3已经由代码手正式复现；
- 论文手原型数值已经冻结；
- M3或`lambda=100`是使用全年数据证明的全局最优选择；
- ORACLE是实际可实施策略；
- 即时交易价敏感性只是对主策略事后换价；
- Q3的S111和原Shapley结果可直接沿用至Q4。
